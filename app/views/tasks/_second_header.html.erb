<header>
  <div class="navbar">
    <% if user_signed_in? %>
     <div class="header-name"><%= link_to current_user.name, "#" %></div>
    <% else %>
      <div class="none"><%= link_to "---", "#" %></div>
    <% end %>
    <div class="list">
    <%= link_to "リストを作成", new_task_path %></div>
  </div>
    <div class="title" style="text-align: center;"><%= link_to "+asquest", root_path %></div>
    <div class="rog">
    <% unless user_signed_in? %>
     <div class="new"><%= link_to "新規登録/ログイン",new_user_session_path %></div>
    <% end %>
    <div class="rog-out"><%= link_to "ログアウト", destroy_user_session_path, class: "nav-link" %></div>
  </div>
</header>